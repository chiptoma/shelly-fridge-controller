{
  "name": "shelly-fridge-controller",
  "version": "3.0.0",
  "description": "Production-grade thermostat controller for Shelly Plus 1/1PM with comprehensive test coverage",
  "main": "script.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --coverageReporters=text --coverageReporters=html",
    "test:ci": "jest --coverage --coverageReporters=text",
    "build": "esbuild src/boot/main.ts --bundle --outfile=dist/main.js --target=es2015 --platform=neutral",
    "build:dev": "esbuild src/main.ts --bundle --outfile=script.tmp.dev.js --format=iife --platform=node --target=es2015 --supported:arrow=false --supported:destructuring=false --supported:template-literal=false --supported:for-of=false --supported:object-rest-spread=false --charset=utf8 --sourcemap && node unwrap-bundle.js script.tmp.dev.js script.dev.js && rm script.tmp.dev.js",
    "build:fast": "esbuild src/main.ts --bundle --minify --outfile=script.tmp.js --format=iife --platform=node --target=es2015 --supported:arrow=false --supported:destructuring=false --supported:template-literal=false --supported:for-of=false --supported:object-rest-spread=false --charset=utf8 && node unwrap-bundle.js script.tmp.js script.js && rm script.tmp.js",
    "validate": "node validate-shelly-script.js",
    "clean": "rm -rf coverage script.js script.dev.js"
  },
  "keywords": [
    "shelly",
    "thermostat",
    "fridge",
    "controller",
    "iot"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.28.5",
    "@babel/preset-env": "^7.28.5",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.25",
    "babel-jest": "^29.7.0",
    "esbuild": "^0.19.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.5",
    "typescript": "^5.9.3"
  }
}
